# Адрес aether-auth со стороны сервера Next (для rewrites). По умолчанию http://localhost:8000
AUTH_INTERNAL_URL=http://localhost:8000

# Браузер ходит на тот же origin (Next проксирует) — оставь пустым.
# NEXT_PUBLIC_AUTH_URL=
